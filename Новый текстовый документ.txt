.elem {
  position: fixed;
  z-index: 500; /* Чтобы убедиться, что он над всеми остальными*/
  /* Центрируем блок */
  left: 50%;
  top: 50%;
  transform: translate(-50%, -50%);
  
  box-shadow: 0 0 0 100vw rgba(0, 0, 0, .6);
}